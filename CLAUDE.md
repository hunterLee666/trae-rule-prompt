# 项目 AI 协作核心宪法
> **TRAE IDE 自动调度版 - 已集成并强制生效**

---

## ✅ 生效状态确认

本项目的 AI 协作规则已 **100% 集成到 TRAE IDE 原生规则体系中**：

| 配置项 | 状态 | 位置 |
| :--- | :--- | :--- |
| 核心宪法规则 | ✅ 强制生效 | `.trae/rules/zh/constitution.md` |
| 自动注入机制 | ✅ 已启用 | `always_applied_workspace_rules` |
| 技能自动调度 | ✅ 已配置 | 无需用户输入命令 |
| 违规拦截 [Rule-403] | ✅ 已激活 | 全程检测越界行为 |

---

## 🎯 核心设计：无命令，自动调度

### 用户体验升级
**旧版本**：用户需要记忆并输入 `/s-brainstorm`、`/s-tdd`、`/e-test-e2e` 等命令
**新版本**：✅ **AI 自动识别场景，自动调用对应技能**

> 用户只需正常描述需求，AI 会根据任务类型自动进入对应工作流！

---

## 🤖 AI 自动工作流演示

### 场景 1：开发新功能
**用户输入**："帮我实现用户登录功能"

**AI 自动执行**：
1. ✅ 自动调用 `brainstorm` 澄清需求细节
2. ✅ 自动调用 `write-plan` 制定实施计划
3. ✅ 用户确认后，自动进入 `tdd` 模式开发
4. ✅ 代码完成后，**自动触发** ECC 安全扫描

### 场景 2：修复 Bug
**用户输入**："修复支付回调超时问题"

**AI 自动执行**：
1. ✅ 自动进入 `tdd` 模式
2. ✅ 先写失败的测试用例 → 修复代码 → 验证通过
3. ✅ 完成后自动进行代码评审

### 场景 3：代码完成后
**用户输入**：无（自动触发）

**AI 后台自动执行**：
1. ✅ `security-reviewer` 安全漏洞扫描
2. ✅ 核心功能 → 自动调用 `e2e` 生成 E2E 测试
3. ✅ API 变更 → 自动调用 `doc-updater` 更新文档

---

## 🔒 权责锁定机制（全程生效）

| 体系 | 独占领域 | 越界拦截 |
| :--- | :--- | :--- |
| **Superpowers 主链路** | 需求规划、TDD 单元测试、集成测试、核心代码评审 | ❌ ECC 禁止编写单元测试 |
| **ECC 辅链路** | E2E 测试、安全扫描、性能优化、文档生成 | ❌ Superpowers 禁止生成 Playwright 代码 |

**[Rule-403] 违规拦截**：AI 检测到越界行为时，立即停止并提示违规。

---

## 📁 文件结构说明

```
.trae/rules/zh/
├── constitution.md    # 项目核心宪法（强制生效，本文件精髓在此！）
├── agents.md          # 代理编排规则
├── coding-style.md    # 编码风格
├── development-workflow.md  # 开发工作流
├── testing.md         # 测试规范
├── security.md        # 安全规范
└── README.md          # 规则说明
```

---

## 🚀 快速开始

**什么都不用做！** 规则已经通过 `always_applied_workspace_rules` 自动注入到每一次 AI 响应的上下文中。

你只需要像平常一样描述你的需求，AI 会：
- ✅ 自动选择正确的技能
- ✅ 自动遵循权责锁定
- ✅ 自动进行合规检查

---

**规则版本**：v3.0 (TRAE IDE 原生自动调度版)
**生效时间**：立即生效
**验证状态**：✅ 所有配置已完成并验证
